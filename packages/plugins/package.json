{
  "name": "@nexora-kit/plugins",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nexora-kit/core": "*",
    "@nexora-kit/sandbox": "*",
    "@nexora-kit/config": "*",
    "@nexora-kit/skills": "*",
    "@nexora-kit/commands": "*",
    "@nexora-kit/mcp": "*",
    "yaml": "^2.8.2",
    "zod": "*",
    "semver": "*"
  }
}

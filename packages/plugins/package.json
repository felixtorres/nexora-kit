{
  "name": "@nexora-kit/plugins",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nexora-kit/commands": "*",
    "@nexora-kit/config": "*",
    "@nexora-kit/core": "*",
    "@nexora-kit/mcp": "*",
    "@nexora-kit/sandbox": "*",
    "@nexora-kit/skills": "*",
    "jiti": "^2.6.1",
    "semver": "*",
    "yaml": "^2.8.2",
    "zod": "*"
  }
}

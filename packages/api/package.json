{
  "name": "@nexora-kit/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nexora-kit/admin": "*",
    "@nexora-kit/core": "*",
    "@nexora-kit/plugins": "*",
    "@nexora-kit/storage": "*",
    "ws": "^8.19.0",
    "zod": "*"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "vitest": "^4.0.0"
  }
}

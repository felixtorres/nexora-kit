{
  "name": "@nexora-kit/testing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nexora-kit/core": "*",
    "@nexora-kit/llm": "*",
    "@nexora-kit/plugins": "*",
    "@nexora-kit/config": "*",
    "@nexora-kit/sandbox": "*",
    "@nexora-kit/skills": "*",
    "@nexora-kit/commands": "*",
    "@nexora-kit/mcp": "*",
    "@nexora-kit/storage": "*",
    "@nexora-kit/admin": "*",
    "@nexora-kit/api": "*",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}

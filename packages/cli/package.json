{
  "name": "@nexora-kit/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "nexora-kit": "dist/bin.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nexora-kit/admin": "*",
    "@nexora-kit/api": "*",
    "@nexora-kit/commands": "*",
    "@nexora-kit/config": "*",
    "@nexora-kit/core": "*",
    "@nexora-kit/llm": "*",
    "@nexora-kit/mcp": "*",
    "@nexora-kit/plugins": "*",
    "@nexora-kit/sandbox": "*",
    "@nexora-kit/skills": "*",
    "@nexora-kit/storage": "*",
    "fflate": "^0.8.2",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}

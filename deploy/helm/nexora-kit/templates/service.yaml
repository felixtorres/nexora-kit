apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexora-kit.fullname" . }}
  labels:
    {{- include "nexora-kit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexora-kit.selectorLabels" . | nindent 4 }}

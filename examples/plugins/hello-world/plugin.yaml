name: hello-world
version: 1.0.0
namespace: hello
description: A minimal example plugin that greets users
author: nexora-kit-team

permissions:
  - llm:invoke

config:
  schema:
    greeting:
      type: string
      description: Default greeting message used by the hello skill
      default: "Hello! I'm the hello-world bot."

name: greet
description: Greet the user with a friendly message
invocation: model
parameters:
  userName:
    type: string
    description: The user's name
prompt: |
  Greet {{userName}} warmly. Incorporate the configured greeting: {{config.greeting}}
  Keep it under 2 sentences.

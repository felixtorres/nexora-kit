name: faq
description: Search the FAQ knowledge base
args:
  - name: query
    type: string
    required: true
    description: Search query
  - name: category
    type: string
    required: false
    description: Filter by category
aliases:
  q: query
  c: category
